# chat-example-ionic

Needs

- nodejs >= 4.0.0
- npm
- ionic (`sudo npm install -g ionic`)

Installation

```bash
$ sudo npm install -g cordova gulp
$ npm install
$ gulp install
```

Run application

`$ ionic serve`
